services:
  - type: web
    name: curable-catalysts-demo
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /americas-next-top-curable-blockchain.html
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
