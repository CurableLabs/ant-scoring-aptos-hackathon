services:
  - type: web
    name: ant-scoring-frontend
    env: static
    buildCommand: echo "No build needed - static site"
    staticPublishPath: .
    routes:
      - type: rewrite
        source: /*
        destination: /index.html

